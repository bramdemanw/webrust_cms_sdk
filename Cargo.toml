[package]
name = "webrust_cms_sdk"
version = "0.1.0"
edition = "2024"
description = "SDK for building WebRust CMS plugins"
license = "MIT"

[dependencies]
extism-pdk = "1.3"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
